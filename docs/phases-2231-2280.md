# Dashboard phases 2231–2280

Orchestrator / Edge / Intelligence hub cross-links, Stripe billing portal, and production deploy helpers.

## Theme

| Phase range | Feature |
|-------------|---------|
| 2231–2235 | Health (Orchestrator) hub → Fleet, Intelligence, Edge |
| 2236–2240 | Intelligence hub → Orchestrator, Fleet, Edge |
| 2241–2245 | Fleet hub → Orchestrator, Intelligence, Edge |
| 2246–2250 | Settings → Access Control (RBAC) hub link |
| 2251–2255 | Stripe billing portal API + Hosted SaaS UI |
| 2256–2260 | Ingress/TLS deploy helper (`deploy-reference-ingress.sh`) |
| 2261–2265 | LDAP deploy helper (`deploy-with-ldap.sh`) |
| 2266–2270 | E2E + docs |

## Test IDs

- `health-context-fleet-link`, `health-context-intelligence-hub-link`, `health-context-edge-link`
- `intelligence-context-orchestrator-link`, `intelligence-context-fleet-link`, `intelligence-context-edge-link`
- `fleet-context-orchestrator-link`, `fleet-context-intelligence-link`, `fleet-context-edge-link`
- `settings-context-rbac-link`
- `hosted-stripe-checkout-button`, `hosted-stripe-portal-button`, `hosted-billing-panel`

E2E: `web/dashboard/tests/phases-2231-2280-features.spec.ts`

## Deploy

```bash
# Ingress + TLS
AETHER_DEPLOY_ENV=~/aether-ingress.env ./scripts/deploy-reference-ingress.sh HOST USER

# Mock IdP SSO (reference cluster)
AETHER_DEPLOY_ENV=~/aether-sso.env ./scripts/deploy-reference-sso.sh HOST USER

# Active Directory
AETHER_DEPLOY_ENV=~/aether-ldap.env ./scripts/deploy-with-ldap.sh HOST USER
```

## Stripe

- `POST /api/hosted/billing/stripe/checkout` — subscription checkout
- `POST /api/hosted/billing/stripe/portal` — customer billing portal (after checkout stores `stripe_customer_id`)
